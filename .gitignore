# dependencies
node_modules/

# secrets — never commit your real .env
.env
.env.local

# runtime database (created on first start, re-seeds itself)
data/

# logs / OS cruft
*.log
.DS_Store

flytap-v2-section-zebra.zip
Archive/

flytap-v2-section-zebra.zip
Archive/

server.bak/
ngrok.exe
ngrok.zip

# WhatsApp pairing credentials (Baileys transport)
data/baileys-auth/

# local copies that must never reach GitHub: an old database, a test bundle, the drop itself
data.old/
webmcp-v2-tests.zip
mar-reference-build-*.zip
